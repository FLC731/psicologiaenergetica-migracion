document.addEventListener('DOMContentLoaded',()=>{const nav=document.getElementById('navigation');if(!nav)return;const items=[['Bienvenida','bienvenida.html'],['EMDR','emdr.html'],['Psicología energética','psicoenergetica.html'],['Recursos terapéuticos','recursos.html']];for(const [label,path] of items){if([...nav.querySelectorAll('a')].some(a=>a.getAttribute('href')?.endsWith(path)))continue;const a=document.createElement('a');a.href='./'+path;a.textContent=label;nav.append(a);}});